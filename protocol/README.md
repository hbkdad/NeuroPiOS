# protocol — Protocol Schema Source of Truth (planned)

Reserved for machine-readable protocol schema definitions (e.g. JSON Schema / protobuf for `docs/PROTOCOL.md`'s objects) once the wire format is finalized in Phase 3. Human-readable spec lives in `docs/PROTOCOL.md` today. **Status: not started.**
