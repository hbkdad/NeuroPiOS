# benchmarks — AION Benchmark Suite Corpus (planned, Phase 5)

Will hold hidden/rotating benchmark definitions per the Benchmark object schema in `docs/PROTOCOL.md`. **Status: not started** — no real benchmark corpus exists yet; `docs/POIG-SPEC.md`'s Proof of Improvement logic is validated in `simulations/poig_sim` only against synthetic scores, not a real corpus.
