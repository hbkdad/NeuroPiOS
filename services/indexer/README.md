# services/indexer — Chain/Network Event Indexer (planned, Phase 8)

Read-only projection of chain events + P2P telemetry into a queryable store for `apps/web` and `apps/explorer`. Never a source of truth for settlement (see `docs/ARCHITECTURE.md`). **Status: not started.**
