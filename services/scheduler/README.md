# services/scheduler — Job Routing/Scheduling Service (planned, Phase 7)

Implements the deterministic Routing Score from `docs/ARCHITECTURE.md`. No learned/neural routing until real production routing data exists (explicit brief constraint). **Status: not started.**
