# packages/types — Shared TypeScript Types (planned)

TS mirror of `docs/PROTOCOL.md` schemas (job object, model manifest, benchmark object) for use across `apps/web`, `apps/node-desktop`, `packages/sdk`. **Status: not started.**
