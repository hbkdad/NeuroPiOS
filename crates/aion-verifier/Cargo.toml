[package]
name = "aion-verifier"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "AION verification tiers L0-L6 (docs/VERIFICATION.md): tier confidence, adaptive verification rate, L1 commitment checks, L2 replicated-execution tolerance comparison"

[dependencies]
aion-crypto = { path = "../aion-crypto" }
thiserror = "1"
