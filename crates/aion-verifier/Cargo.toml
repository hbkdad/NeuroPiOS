[package]
name = "aion-verifier"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "AION verification tiers L0-L6 (docs/VERIFICATION.md): tier confidence, adaptive verification rate, L1 commitment checks, L2 replicated-execution tolerance comparison, L3 deterministic wasm execution, L4 dispute state machine, L6 commit/reveal quorum"

[dependencies]
aion-crypto = { path = "../aion-crypto" }
thiserror = "1"
wasmi = "1.1.0"

[dev-dependencies]
wat = "1.254.0"
