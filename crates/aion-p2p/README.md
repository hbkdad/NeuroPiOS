# crates/aion-p2p — P2P Networking (planned, Phase 3)

rust-libp2p wrapper: GossipSub (job/announcement pub-sub), Kademlia + Identify (peer/content discovery), Noise (transport security), QUIC primary / TCP fallback transport, AutoNAT + circuit relay (NAT traversal), peer scoring and connection/resource limits (anti-spam). See `docs/adr/0002-p2p-stack.md`. **Status: not started.**
