# crates/aion-cli — `aion` CLI (planned, Phase 3+)

Target UX: `aion devnet up|status|down` (local multi-node dev network per the orchestration brief), plus node/job/model management subcommands. **Status: not started.**
