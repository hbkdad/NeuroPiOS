# apps/docs — AION Documentation Site (planned)

Will publish the contents of `/docs` as a browsable site. **Status: not started** — `/docs` itself (plain Markdown, read directly from the repo) is the authoritative source in the meantime.
